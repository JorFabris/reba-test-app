import {StyleSheet} from 'react-native';
import {Colors} from '../../assets/Colors';

const styles = StyleSheet.create({
  title: {
    fontWeight: 'bold',
    fontSize: 18,
    color: Colors.textColor,
    margin: 15,
  },
  screen: {marginBottom: 130},
});

export default styles;
